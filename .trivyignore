# ─── Trivy ignore file ────────────────────────────────────────────────────────
#
# These CVEs are ignored because they affect packages bundled inside the Node.js
# base image tooling (npm CLI at usr/local/lib/node_modules/npm/) that are NOT
# executed at runtime in production containers.
# They will be resolved automatically when a new node:24-alpine base image is
# published with an updated npm version.

# tar@7.5.9 — path traversal via hardlinks/symlinks (npm bundled only)
CVE-2026-29786
CVE-2026-31802

# minimatch@10.2.2 — ReDoS (npm bundled only)
CVE-2026-27903
CVE-2026-27904

# picomatch@4.0.3 — ReDoS / method injection (overridden to >=4.0.4)
CVE-2026-33671
CVE-2026-33672

# brace-expansion — DoS via zero step (overridden to >=1.1.13 in pnpm overrides)
CVE-2026-33750

# hono / @hono/node-server — via @prisma/dev (Prisma internal devDep, not called at runtime)
CVE-2026-39406
CVE-2026-39407
CVE-2026-39408
CVE-2026-39409
CVE-2026-39410
GHSA-26pp-8wgv-hjvm

# nodemailer — SMTP CRLF injection (updated to 8.0.5)
GHSA-vvjj-xcjg-gr5g

# zlib@1.3.1-r2 — buffer overflow in untgz CLI utility (not libz used by Node.js)
CVE-2026-22184
