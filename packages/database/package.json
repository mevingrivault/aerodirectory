{
  "name": "@aerodirectory/database",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "dotenv -e .env -e .env.example -- prisma migrate dev",
    "prisma:seed": "dotenv -e .env -e .env.example -- tsx src/seed.ts",
    "lint": "tsc --noEmit",
    "test": "echo 'No tests yet'"
  },
  "dependencies": {
    "@prisma/client": "^6.2.0"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.4",
    "prisma": "^6.2.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
